/**
 * Give the random number between 1 and 0
 */
console.log(Math.random());

/**
 * Give the max value
 */
console.log(Math.max(1, 2, 3, 4));

/**
 * For more information visit https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math
 */
