/**
 * Expression;
 * do { statement }
 * while ( condition );
 * Difference from while loop is, always execute at least once even the
 * condition is false.
 */

let i = 9;

do{
    console.log(i);
}
while(i <= 5);