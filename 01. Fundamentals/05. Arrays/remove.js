let arr = [1, 2, 3, 4, 5];

/**
 * Remove the first item of array
 */
arr.pop();

/**
 * Remove the last item of array
 */
arr.shift();

/**
 * Remove somewhere in the middle of array
 * 1 = index
 * 2 = delete count
 */
arr.splice(1, 2);

console.log(arr);
