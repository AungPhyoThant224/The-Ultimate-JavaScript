// Perform a task
function greet(firstName, lastName){
    console.log("Hello " + firstName + " " + lastName);
}

greet('John', 'Smith');

// Calculate a value
function square(number){
    return number * number;
}

console.log(square(2));
/* In above line console.log() is a function and square(2) is also a function
    so that is two functions call.
*/